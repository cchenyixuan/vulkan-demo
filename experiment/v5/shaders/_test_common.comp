#version 460
#extension GL_GOOGLE_include_directive : enable
#include "common.glsl"

// Minimal test shader to verify common.glsl compiles.
// Body intentionally empty (all bindings unused so SPIR-V strips them).

layout(local_size_x_id = 51, local_size_y = 1, local_size_z = 1) in;

void main() {
}
